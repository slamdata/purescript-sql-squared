{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict --stash",
    "test": "pulp test"
  },
  "dependencies": {
    "pulp": "^12.2.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.6.0"
  }
}
