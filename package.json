{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict --stash",
    "test": "pulp test"
  },
  "dependencies": {
    "pulp": "^15.0.0",
    "purescript": "^0.14.1",
    "purescript-psa": "^0.8.2"
  }
}
