{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict --stash",
    "test": "pulp test"
  },
  "dependencies": {
    "pulp": "^11.0.0",
    "purescript": "^0.11.4",
    "purescript-psa": "^0.5.1"
  }
}
